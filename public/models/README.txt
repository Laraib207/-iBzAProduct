Place product 3D model files here using the product id as file name.

Examples:

- aero-shift.glb
- apex-guard.glb
- volt-surge.glb

For iOS AR quick-preview, optionally include .usdz versions with the same name.

When present, the product detail page will attempt to load /models/<id>.glb via <model-viewer>.
